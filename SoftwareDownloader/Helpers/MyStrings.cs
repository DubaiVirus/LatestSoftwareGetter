using System;

namespace SoftwareDownloader.Helpers
{
    public class MyStrings
    {
        public static readonly string XmlFileLocation = $@"{Environment.GetFolderPath(Environment.SpecialFolder.CommonApplicationData)}\foreignWay\SoftwareDownloader\config.xml";
    }
}
